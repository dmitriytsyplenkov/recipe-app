spring:
  datasource:
    url: jdbc:mysql://localhost:3306/recipeapp_prod
    username: recipeapp_prod_user
    password: dts
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL8InnoDBDialect
    database: mysql
    show-sql: false
